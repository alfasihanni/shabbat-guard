# שומר שבת לקמפיינים — ⚠️ הריפו הזה כבר לא מריץ כלום

**מ-28.8.2026 השומר רץ על Railway, לא כאן.** ה-workflow בריפו הזה **מושבת בכוונה**.
אל תדליק אותו — שני מנגנונים שמכבים ומדליקים את אותם קמפיינים יתנגשו.

## למה עברנו

ב-28.8.2026 הקמפיינים לא כובו ב-14:00. הקוד, הטוקן וה-workflow היו תקינים —
**ה-cron של GitHub פשוט הפסיק לירות**, בלי שגיאה ובלי התראה:

| תאריך | ריצות באותו יום |
|---|---|
| 25.8 | 34 |
| 26.8 | 21 |
| 27.8 | **3** |
| 28.8 | **1** (ב-05:23 UTC), ואז שקט של 7 שעות |

`gh workflow list` הראה `active`. githubstatus.com הראה `All Systems Operational`.
`schedule` ב-GitHub Actions הוא best-effort לפי התיעוד של GitHub עצמו, ולכן הוא
לא מתאים למשהו שאסור לו להיכשל.

## איפה זה רץ עכשיו

| | |
|---|---|
| אירוח | Railway, פרויקט `shabbat-guard` = `4e931c11-ea33-4d37-a163-e9aecb6029b7` |
| שירות | `guard` = `5bb43273-2c19-4fc0-befc-cd096a89f8a1` |
| כתובת | `https://guard-production-eee7.up.railway.app/health` |
| כונן קבוע | `guard-volume` ב-`/data` — **חובה**, שם יושב `state.json` |
| קוד מקור | `C:\Users\חני\HanniAutomation\shabbat-guard-railway` |
| סטטוס בלחיצה | `HanniAutomation\shabbat-status.bat` |

ההיגיון זהה למה שהיה כאן: כיבוי ב-14:00 ביום הדלקת הנרות, הדלקה בהבדלה לפי Hebcal
(תל אביב). ההבדל היחיד הוא מי מריץ אותו — תהליך שרץ ברציפות ובודק כל דקה, במקום
תור משימות של GitHub.
